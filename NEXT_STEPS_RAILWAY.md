# 🚀 Следующие шаги для деплоя на Railway

## ✅ Текущий статус

- ✅ Код запушен в GitHub: `rbr-ntc/survey-assessment-backend`
- ✅ Railway CLI установлен и залогинен
- ✅ Подключен к проекту: `perfect-clarity`

## 📋 Что нужно сделать

### 1. Создать/выбрать сервис для Backend

В Railway Dashboard:

1. Откройте проект `perfect-clarity`
2. Если сервиса для backend еще нет:
   - Нажмите **New** → **GitHub Repo**
   - Выберите: `rbr-ntc/survey-assessment-backend`
   - Railway автоматически определит Dockerfile
3. Если сервис уже есть - выберите его

### 2. Добавить PostgreSQL базу данных

1. В проекте Railway: **New** → **Database** → **PostgreSQL**
2. Подождите создания базы (1-2 минуты)
3. Railway автоматически создаст переменные:
   - `DATABASE_URL` (будет преобразован в `POSTGRES_URL`)

### 3. Настроить переменные окружения

В настройках Backend сервиса (Settings → Variables) добавьте:

**Минимум для работы:**
```bash
API_KEY=<openssl rand -hex 32>
SECRET_KEY=<openssl rand -hex 64>
MONGO_URL=<ваш MongoDB URL>
OPENAI_API_KEY=<ваш ключ>
CORS_ORIGINS=https://your-frontend.vercel.app
```

**Полный список:** см. `ENV_RAILWAY.md`

### 4. Дождаться деплоя

Railway автоматически задеплоит при push в main (уже запушено).

Проверьте логи в Railway Dashboard → ваш сервис → **Deployments**

### 5. Применить миграции

После успешного деплоя:

**Через Railway CLI:**
```bash
cd /Users/mistadrumma/develop/cursor/survey-assessment-backend

# Убедитесь, что подключены к правильному сервису
railway service  # покажет текущий сервис

# Если нужно переключиться на backend сервис:
railway link  # выберите backend сервис

# Примените миграции
railway run alembic upgrade head
```

**Или через Railway Shell:**
1. Railway Dashboard → ваш сервис → **Shell**
2. Выполните: `alembic upgrade head`

### 6. Проверить деплой

1. **Health check:**
   ```
   https://your-backend.railway.app/health
   ```

2. **Swagger docs:**
   ```
   https://your-backend.railway.app/docs
   ```

3. **Auth endpoints:**
   ```
   POST https://your-backend.railway.app/api/v1/auth/register
   ```

## 🔧 Полезные команды Railway CLI

```bash
# Просмотр текущего проекта/сервиса
railway status

# Переключение на другой сервис
railway link

# Просмотр логов
railway logs

# Просмотр переменных
railway variables

# Добавление переменной
railway variables set KEY=value

# Просмотр URL
railway domain
```

## 📝 Чеклист

- [ ] Backend сервис создан/выбран в Railway
- [ ] PostgreSQL база данных добавлена
- [ ] Все переменные окружения настроены
- [ ] Деплой успешно завершен
- [ ] Миграции применены (`alembic upgrade head`)
- [ ] Health endpoint работает
- [ ] Swagger docs доступны
- [ ] Auth endpoints работают

---

**После успешного деплоя можно переходить к frontend! 🎨**

